<h1> Recent Jobs </h1>

<% @jobs.each do |job|%>    
    <p>
        <%= link_to job.title, job_path(job) %>                
    </p>
    <p>
        <%= job.company %> - <%= job.location%>        
    </p>    
<% end %>